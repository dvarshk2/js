var cl=console.log;
var s1="Hello how are you";
var str="I Love javaScript";
cl(str);
//reverse string using for loop
var newStr='';

for(var i=str.length - 1;i>=0;i--){
	newStr += str.charAt(i);
}
cl(newStr);

// var newResult='';
// function revString(str){
	// for(var i=str.length - 1;i>=0;i--){
	// newStr += str.charAt(i);
// }
// }
// revString(str);
// revString(s1);

//reverse string using array methods
var arr=str.split('');
cl(arr);
var revArr = arr.reverse();
cl(revArr);
var revstr=revArr.join('');
cl(revstr);

var revstr=str.split('').reverse().join('');//chaining of methods
cl(revstr);

function reverseStr(str){
	return str.split('').reverse().join('');
}

cl(reverseStr(str));